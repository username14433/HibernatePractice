# HibernatePractice
This is a console Java application where I practiced using the Hibernate ORM
# 🎓 Hibernate Practice

## 📘 Описание проекта  
Этот проект демонстрирует использование **Hibernate ORM** для работы с объектно-реляционным отображением (ORM) в Java.  
Он моделирует систему учёта студентов, их адресов, групп и факультативных секций.  
В качестве базы данных используется **PostgreSQL**, развёрнутая в **Docker** с постоянным хранилищем (**Docker Volumes**), что обеспечивает сохранение данных между перезапусками контейнера.

---

## ⚙️ Используемые технологии

| Технология | Назначение |
|-------------|------------|
| ☕ **Java 17+** | Основной язык разработки |
| 🧩 **Hibernate ORM (JPA)** | Объектно-реляционное отображение данных |
| 🐘 **PostgreSQL** | Реляционная база данных |
| 🐳 **Docker + Docker Volumes** | Контейнеризация и сохранение данных |
| 📦 **Maven** | Система сборки и управления зависимостями |

---

## 🏗️ Архитектура проекта  

src/  

└── main/  
└── java/  
└── org/  
└── rockend/  
├── HibernateExample.java // Главный класс для запуска  
├── config/  
│ └── HibernateConfig.java // Конфигурация Hibernate и подключение к БД  
└── entity/  
├── Student.java // Сущность студента  
├── Address.java // Сущность адреса  
├── Group.java // Сущность группы  
├── Section.java // Сущность секции (факультатива)  
├── SectionType.java // Перечисление типов секций  
└── FacultyType.java // Перечисление типов факультетов  

---

## 🧩 Связи между сущностями  

| Связь | Описание |
|-------|-----------|
| **Student ↔ Address** | One-to-One — каждый студент имеет один адрес |
| **Student ↔ Group** | Many-to-One — несколько студентов могут принадлежать одной группе |
| **Student ↔ Section** | Many-to-Many — студент может посещать несколько секций, и секция может включать нескольких студентов |

> Все связи **двунаправленные**, что позволяет выполнять запросы в обе стороны (например, получать студентов по секции и наоборот).

---

## 🐳 Работа с базой данных через Docker  

Для запуска PostgreSQL с сохранением данных используется Docker Volume.

### Пример команды для создания volume и запуска контейнера:

```bash
docker volume create --name my_volume
docker run --name postgres-container \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=university_db \
  -v my_volume:/var/lib/postgresql/data \
  -p 5438:5432 \
  -d postgres:latest
После запуска контейнера Hibernate автоматически создаёт таблицы и управляет ими через ORM.

🚀 Запуск проекта
Клонируйте репозиторий:


git clone https://github.com/username14433/HibernatePractice.git
cd HibernatePractice
Убедитесь, что контейнер PostgreSQL запущен и доступен.

Соберите и запустите проект:

mvn clean install
mvn exec:java -Dexec.mainClass="org.rockend.HibernateExample"
🧾 Навыки, продемонстрированные в проекте
Работа с Hibernate ORM и аннотациями JPA

Настройка One-to-One, Many-to-One, Many-to-Many связей

Конфигурация Hibernate через Java Config (без XML)

Подключение к PostgreSQL в Docker

Использование Maven для управления зависимостями и сборки проекта

Логирование SQL-запросов и автоматическое создание таблиц

👨‍💻 Автор
<PRIVATE_PERSON>
💬 Java Developer | Spring | Hibernate | SQL | Docker | PostgreSQL
